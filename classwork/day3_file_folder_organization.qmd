---
title: "Computation for Linguists"
subtitle: "How to Organize Files & Folders for Computation"
author: "Dr. Andrew M. Byrd"
date: "August 28, 2026"
format:
  revealjs:
    css: header_shrink.css
    theme: beige
    slide-number: true
    center: true
    toc: true
    toc-title: "Plan for the Day"
    toc-depth: 1
editor: visual
---

# Review

## Review from Day 2

-   We set up VSCode + Git, confirmed our SSH connection, cloned the shared course repo, and created + cloned our own personal repos
-   We stopped right after cloning our own repo alongside the course repo — we haven't tested the full pipeline yet, so that's first up today

# Finishing Up From Day 2

## One Folder, Both Repos

```         
computation_4_linguists/
├── 301_2026/      (shared course repo — pull for updates)
└── LIN_301/       (your repo — where you push your work)
```

-   Open `computation_4_linguists` in VSCode (**File → Open Folder**) and you'll see both repos in the Explorer at once
-   This is the folder you'll open every class from now on

# Testing the Whole Pipeline

## Edit, Commit, Push

1.  Open `README.md` in the VSCode editor, add a sentence, save (`Ctrl+S` / `Cmd+S`)
2.  Click the **Source Control** icon (branch icon, left sidebar)
3.  Hover over the changed file, click **+** to stage it
4.  Type a commit message (e.g. "first commit")
5.  Click the checkmark (✓) to commit

::: callout-important
## This Is Crucial!

Committing only saves the change *locally*. You must also click **Sync Changes** (or **... → Push**) to actually send it to GitHub!
:::

## Verify It Worked

-   Go to your repo on github.com
-   Refresh the page
-   You should see your edited `README.md` with your new sentence

# Git Practice

## Part 1: Add and Push a File

1.  In your `LIN_301` repo, create a new file called `setup_notes.md`, describing what you've done so far in setting up VSCode and Git
2.  Save, then check **Source Control** — notice this file is marked differently than `README.md` was earlier (untracked, not modified)
3.  Stage → commit → push

## Part 2: Cause (and Fix) a Merge Conflict

Goal: see a conflict happen on purpose, in a safe spot, before it happens to you by accident.

1.  On github.com, open your `LIN_301` repo → click into `setup_notes.md` → click the pencil icon to edit → add a line, then commit the change directly on GitHub
2.  Back in VSCode — **without pulling first** — edit `setup_notes.md` locally, adding a *different* line. Save, stage, commit.
3.  Try to push. What happens?
4.  Pull. VSCode will report a conflict — open the file and look for the conflict markers: `<<<<<<<`, `=======`, `>>>>>>>`
5.  Decide what the file should actually say, remove the markers and leave only the final version, then stage, commit, and push
6.  Refresh github.com to confirm your resolved version made it up

# Make VSCode Yours

## Activity: Customize Your Setup

-   Open Settings: gear icon (bottom-left) → **Settings**, or `Ctrl+,` / `Cmd+,`
-   Try at least two of the following:
    -   **Color Theme** — `Ctrl+K Ctrl+T` / `Cmd+K Cmd+T`, browse and pick one
    -   **Font Size** — search "font size" in Settings, adjust `Editor: Font Size`
    -   **Auto Save** — File → Auto Save (turn it on — never lose work to a forgotten `Ctrl+S` again)
    -   **Icon Theme** — Command Palette → "File Icon Theme," try a different set
-   At home: you can browse the Extensions Marketplace for something fun (a theme pack, an icon pack) and install it
    -   **"Material Icon Theme"** — a more detailed icon set for the Explorer
    -   **"One Dark Pro"** — a popular color theme
    -   **"vscode-pets"** — a small pet that lives in your editor (yes, really)

# Why Good File Organization Matters

## A Preview of What's Coming

-   In a few weeks: LaTeX documents need images, `.bib` files, and style files to be in the *right* place, or they won't compile
-   Even sooner: Python scripts need to find data files, or they'll throw an error
-   Git tracks the exact structure of your project, folder by folder
-   A messy project = hours of your life spent hunting for files instead of doing linguistics

## The Bad Old Way

-   `final_paper_v2_ACTUALFINAL_reallyfinal.docx`, living somewhere in Downloads
-   `Screenshot 2026-08-28 at 3.42.17 PM.png`
-   Everything just dumped on the Desktop
-   Sound familiar?

# Principles of Good File Organization

## Naming Conventions: No Spaces

-   `my data.csv` → the command line and Python may choke on this
-   `my_data.csv` or `my-data.csv` → safe everywhere
-   We'll see exactly why next class, but get in the habit now

## Naming Conventions: Case & Clarity

-   Prefer lowercase — avoids Mac vs. Windows vs. Linux case-sensitivity headaches
-   Be descriptive, not generic:
    -   ~~`data1.csv`~~ → `jfk_speech_tokens.csv`
    -   ~~`hw.qmd`~~ → `hw3_beginning_python.qmd`
-   Use ISO dates (`YYYY-MM-DD`) if a filename needs one — they sort correctly

## Version Numbers vs. Git

-   Old habit: `paper_v1.docx`, `paper_v2.docx`, `paper_FINAL.docx`, `paper_FINAL_v2.docx`
-   New habit: **one file**, `paper.qmd` — and let **Git** track every version for you
-   This is exactly what you set up today. You don't need "v2" in a filename ever again.

## Folder Hierarchy

-   Don't dump every file into one giant folder
-   Group by purpose:

```         
LIN_301/
├── homework/
├── classwork/
├── data/
└── README.md
```

-   Don't go too deep either — 2–3 levels is usually plenty. If you need a map to find a file, it's too deep.

## README Files

-   Every good project folder has a `README.md` at the top
-   It answers: what is this project? how is it organized? how do I use it?
-   You already have one — set up in your Git repo!

# A Real Example

## Look at the Course Repo

-   Open `301_2026/` (inside your `computation_4_linguists` folder) in VSCode's Explorer panel
-   Two top-level folders so far: `slides/` and `activities/` — even this early, materials are split by type, not all dumped together
-   Inside `slides/`, notice the naming pattern: numbered, in teaching order, short description

# Activity

## Activity 1: Set Up Your Own Structure

-   In your `LIN_301` repo, using VSCode's Explorer panel (right-click → **New Folder**), create:
    -   `homework/`
    -   `classwork/`
    -   `data/`
-   Git only tracks **files**, not empty folders — add a small `README.md` inside each one (a sentence on what goes there) so the folder actually has something to commit
-   Copy today's slide file into `classwork/`, renaming it to mark the day:
    ``` bash
    cp ~/301_2026/slides/1_setup_vscode_git/3_file_folder_organization.qmd ./classwork/day3_file_folder_organization.qmd
    ```
    Do this again after each class for the rest of the semester — `classwork/` becomes a running archive, named `dayN_topic.qmd`
-   We'll use this same structure for the rest of the semester — every homework goes in `homework/`, etc.
-   Commit and push this change — README files and today's copied slide included — before you leave (practice from today!)

## Activity 2: Organize a Messy Project

1.  Access the course repo (`301_2026`)
2.  Find `activities/day3_messy_project/messy_downloads_dump/` in the Explorer
3.  **Copy** that whole folder somewhere outside `computation_4_linguists` (e.g. your Desktop) — don't rename or move files inside the shared repo itself, or your next `git pull` will break
4.  In pairs, on your copy: rename every file using today's conventions, create folders that make sense, and move files into them
5.  Be ready to explain your choices to the class

# Wrap-Up

## Big Takeaways

-   Good naming and structure isn't pedantic — it prevents real bugs later in the semester
-   Consistency matters more than any single "correct" system
-   Git + a clean structure means you can always find — and recover — your work

## Next Time

-   Learning the Command Line: navigating, creating, and moving files from the terminal